// Root is just an aggregator. The framework lives in :nativegen-* and :annotations
// /:processor; runnable demos live under :examples:*; native cores under :native:*.
subprojects {
    repositories {
        mavenCentral()
    }
}
